# ------------------------------------------------------------------------------
# Inline Ruby (to_s + print)

`code`

# ------------------------------------------------------------------------------
# Ruby Line

@: code

# ------------------------------------------------------------------------------
# Ruby Block

@begin
  code
@end

# ------------------------------------------------------------------------------
# Alias Macros

alias@real

# ------------------------------------------------------------------------------
# Inline Macros

@define BAR(x) bar`x`

@ruby
  define :BAR, [:x], 'bar`x`'
@end

# ------------------------------------------------------------------------------
# Statement Macros

@macro foo a, b
  op `a`
  op `b`
@end

@ruby
  define :foo, [:a, :b], [
    '  op `a`',
    '  op `b`'
  ]
@end

movq 2, @f=a0
addl 3, @f

at :$abc
        dfdf
        dfd
        dfd

        cond do |c|
          c.when (:==) {
          }
          c.when (:!=) {
          }
        end


# ------------------------------------------------------------------------------
# Conditional Assembly
                    
@if   ...
@elif ...
@else
@end

# ------------------------------------------------------------------------------
# Repitition

@for f in `f dfdf`
@end

